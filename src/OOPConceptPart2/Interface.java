package OOPConceptPart2;

public interface Interface {

	// cannot create object of Interface
	// Only method declaration -- method prototype--  no method body
	// Can not create static prototype method in Interface
	// interface variables are static and final by default
	
	//from JDK1.8 we can have static methods in Interface with method body
	
	
	
	
	
	
	// There is no main method in Interface
	// we can declare the variables, by default all the variable are static in nature
	// variable values will not be changed
	// no static method in Interface
	// Only method declaration
	// no body for the methods
	// we can not create object for Interface
	// Interface is abstract in nature
}
